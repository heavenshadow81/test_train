using UnityEngine;

namespace ML.PlaywallKids.Aquarium
{
    public class MyEffect : MonoBehaviour
    {
        /*
        public VortexEffect effect;

        void LateUpdate()
        {
            effect.center.x = Mathf.Sin(Time.time);
            effect.center.y = Mathf.Cos(1 - Time.time);
        }
        */
    }
}